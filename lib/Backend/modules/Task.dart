class Task {
  String title, description;
  bool completed;
  DateTime date;
  Task(
      {required this.title,
      required this.description,
      required this.completed,
      required this.date});
}
